{% extends 'dashboard.html.twig' %}


{% block body %}
<body>
<div class="container-xl">
	<div class="table-responsive">
		<div class="table-wrapper">
			<div class="table-title">
				<div class="row">
                
					<div class="col-sm-6">
						<h2>Admin <b>Dashborad</b></h2>

					</div>
					<div class="col-sm-6">
						<a href="{{ url("create_SE",{"id": id}) }}" class="btn btn-success" ><i class="material-icons">&#xE147;</i> <span>Add New </span></a>
					</div>
				</div>
			</div>
            {% for message in app.flashes('success')  %}
                <div class="alert alert-success">
                {{message}}
                </div>
            {% endfor %}
			<table class="table table-striped table-hover">
				<thead>
					<tr>
						
						<th>Image </th>
						<th>Nom </th>
						<th>Localisation</th>
						<th>Description</th>
						<th>Prix</th>
						
					</tr>
				</thead>
				<tbody>
{% for s in SE %}
					<tr>
						
						<td><img width="150" height="100" src="{{ asset('assets/images/salles/' ~ s.img )}}" alt="blog image"></td>
						<td>{{s.nom}}</td>
						<td >{{s.location}}</td>
						<td >{{s.description}}</td>
						<td >{{s.prix}} TND</td>
						
						<td>
							<a href="{{url("update_SE",{"id": s.id})}}" ><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
							<a href=" {{url("delete_SE",{"id": s.id})}}" class="delete" onclick="return confirme()"><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>
						</td>
					</tr>
{% endfor %}					
				</tbody>
			</table>
			
		</div>
	</div>        
</div>


</body>
<script type="text/javascript">
  function confirme( ){
if( confirm( "Voulez vous vraiment supprimer cette évenement ?" )){
}
else{alert("Opération annuler");
             return false;}
}
</script>



{% endblock body %}